services:
  app:
    env_file:
      - ./backend/.env          # injected from GitHub Action
    volumes: []       # disable bind mount
    restart: always

  web:
    ports:
      - "80:80"
    restart: always

  db:
    restart: always

  redis:
    restart: always